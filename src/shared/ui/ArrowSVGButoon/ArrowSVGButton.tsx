import ArrowSVG from '../../../img/logo/heart.svg';

import SVGWrapper from '../../lib/SVGWrapper';
import styles from './ArrowSVGButton.module.scss';

const ArrowSVGButton = () => {
  return <SVGWrapper svgElement={<ArrowSVG />} />;
};
export default ArrowSVGButton;
